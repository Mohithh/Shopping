













// 'use client';

// import { useContext } from "react";
// import { FirstContext } from "../layout"; // Adjust the path if necessary

// const Page = () => {
//   const { first, second, third } = useContext(FirstContext); // Access values from the context

//   return (
//     <div>
//       <h1>Value of First: {first}</h1>
//       <h1>Value of Second: {second}</h1>
//       <h1>Value of Third: {third}</h1>



      {/*  1. instal - npm install mongoose --save */}

       {/* 2. connect with mongoose */}

       {/* 3. create a mongoose Schema  */}

       {/* 4. create a API and import schema and connect mongoose(export name )  */} 

       {/* 5. create api    addproduct . get product updateproduct */}

       {/* 6.import- get product and connect mongoose in shirt page so change hardcodded data  */}

       {/* 7. in shirt page add or working on showing size and color avalibale qty */}

      //   8.   react toastify           route.push 
      
      // 9. crypto-js for hasing password
      
      // 10. json web token 

      //11. useSearchParams - user log in or not --- usestate run always whenever page chnage 

      //  12. react icon , tost , top loading bar 

      // 13. process.env.NEXT_PUBLIC_HOST use envirent variable to change links



      

//     </div>
//   );
// };

// export default Page;




// // // slug-
// import mongoose from "mongoose";
// import Product from "@/models/Products/page"; // Adjust the import path as needed

// Server Component (default in the app directory)
// const Page = async ({ params }) => {
//   const { slug } = params; // Get the slug from the URL params

//   // let products = [];

  
//     // Connect to the MongoDB database
//     if (!mongoose.connections[0].readyState) {
//       await mongoose.connect(process.env.MONGO_URL);
//     }

//     // Fetch products based on category "tshirt"
//     const products = await Product.find({slug });
//     console.log(products)

  

//   return (
//     <div>
     
//      {products.map((item)=> {return <div>
//       {item.title}

//       </div>})}

//     </div>
//   );
// };

// export default Page;






